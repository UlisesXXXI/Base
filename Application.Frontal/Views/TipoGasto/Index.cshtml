@model IEnumerable<Application.bbdd.Entities.Maestros.TipoGasto>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Descripcion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Descripcion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TipoGastoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TipoGastoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TipoGastoID })
        </td>
    </tr>
}

</table>
